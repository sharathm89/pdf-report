## Install in report-app

#### `make install` or `npm install`

## Install in api-server

#### `make install` or `npm install`


## Run in report-app

#### `make run` or `npm start`

## Run in api-server

#### `make run` or `npm start`